<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Temperature Converter</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="tool-container">
    <h1>🌡️ Temperature Converter</h1>

    <form method="POST" action="/temperature">
      <input type="number" name="celsius" placeholder="°C" required>
      <button type="submit">Convert to °F</button>
    </form>

    <% if (result !== null) { %>
      <div class="result">
        Result: <strong><%= result %> °F</strong>
      </div>
    <% } %>

    <div class="history">
      <h3>History:</h3>
      <ul>
        <% history.forEach(item => { %>
          <li><%= item.input %> °C → <%= item.result %> °F</li>
        <% }) %>
      </ul>
    </div>

    <a class="back-button" href="/">⬅ Back to Dashboard</a>
  </div>
</body>
</html>
